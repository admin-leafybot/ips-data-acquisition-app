# Sentry configuration for source bundling
# Replace 'your-org-slug' with your actual Sentry organization slug
defaults.org=ibrain-5r

# Optional: Set default project (if you have multiple projects)
defaults.project=android

# Optional: Set auth token for uploads (if needed)
# auth.token=your-auth-token
